# --- Core Frameworks ---
streamlit >= 1.20.0
fastapi >= 0.90.0
uvicorn[standard] >= 0.20.0

# --- Streamlit Components ---
streamlit-option-menu >= 0.3.0

# --- Data Handling & Manipulation ---
pandas >= 1.5.0
numpy >= 1.20.0

# --- APIs & External Services ---
google-generativeai >= 0.3.0
googletrans >= 4.0.0rc1
python-dotenv >= 1.0.0
requests >= 2.28.0
roboflow >= 1.1.0
gTTS >= 2.2.4  # ✅ Added to fix deployment error

# --- Image Processing & Computer Vision ---
Pillow >= 9.0.0
opencv-python-headless >= 4.5.0
supervision >= 0.16.0

# --- Machine Learning / Deep Learning ---
tensorflow >= 2.10.0
keras >= 2.10.0

# --- PDF & Excel Generation ---
reportlab >= 3.6.0
openpyxl >= 3.1.0  # Optional, useful for Excel writing

# --- Web & File Upload Handling ---
python-multipart >= 0.0.6

# --- Other Tools ---
bcrypt >= 4.0.1

# --- For logging and JSON handling (usually built-in, but no harm pinning)
